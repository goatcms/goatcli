package cliapp

const (
	// AppName is application name
	AppName = "GoatCLI"
	// AppVersion is application version
	AppVersion = "0.0.1"
	// RootPath is path for root filesystem
	RootPath = ""
	// CurrentPath is path for current filesystem
	CurrentPath = "./"
	// CurrentFilesystem is current directory filesystem key
	CurrentFilesystem = "current"
)
